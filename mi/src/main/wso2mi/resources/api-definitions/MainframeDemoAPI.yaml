openapi: 3.0.1
info:
  title: MainframeDemoAPI
  description: API Definition of MainframeDemoAPI
  version: 1.0.0
servers:
- url: http://localhost:8290/mf
- url: https://localhost:8253/mf
paths:
  /account/{id}:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        default:
          description: Default response
  /customer/{id}:
    get:
      parameters:
      - name: id
        in: path
        required: true
        schema:
          type: string
      responses:
        default:
          description: Default response
  /txn:
    post:
      requestBody:
        description: Sample Payload
        content:
          application/json:
            schema:
              type: object
              properties:
                payload:
                  type: object
          application/xml:
            schema:
              type: object
              xml:
                name: payload
        required: false
      responses:
        default:
          description: Default response
